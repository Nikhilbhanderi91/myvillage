name: myvillage
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Default Flutter icons for iOS
  cupertino_icons: ^1.0.8

  # Added dependencies
  shared_preferences: ^2.1.1
  carousel_slider: ^4.2.1
  google_fonts: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
